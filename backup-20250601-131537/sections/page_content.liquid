<section class="section">
  <div class="container">
    {% if page.title %}
      <h1>{{ page.title }}</h1>
    {% endif %}
    {{ page.content }}
  </div>
</section>

{% schema %}
{
  "name": "Page Content",
  "elements": []
}
{% endschema %}
